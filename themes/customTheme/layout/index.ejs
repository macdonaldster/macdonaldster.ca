<div class='row text-center'>
 <h1><%= config.author %></h1>
    <p><%= config.subtitle %></p>

</div>
<div class='row'>
<div class='col-md-8'>
 <ul>
 <% page.posts.each(function(post) { %>
     <li>
      <h3>
                <a href="<%- url_for(post.path) %>">
           <%= post.title %>
          </a>
            </h3>
            <div class='text-muted'>
                <%= date(post.date, config.date_format) %> 
            </div>
            
            <% if(post.excerpt) { %>
                <p>
                    <%- post.excerpt %>
                </p>
            <% } %>
	    		<p><b>&gt;</b> <a href="<%- url_for(post.path) %>">view more...</a></p>

     </li>
   <% }) %>
 </ul>
</div>
<div class='col-md-2'>
<h4>About this site</h4>
<p>Just a list of stuff I like in no particular order. I just want to write about fun, interesting, and useful stuff. Trying to keep the politics off of here.</p>
</div>
</div>